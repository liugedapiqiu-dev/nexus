# 🧠 VectorBrain DAG 任务系统 - 自我诊断报告

**生成时间**: 2026-03-15 00:19  
**系统版本**: DAG v1.0 (基础版)  
**状态**: ✅ 可运行，但存在多个待解决问题

---

## 📊 系统现状

### ✅ 已实现功能

| 功能模块 | 状态 | 完成度 |
|---------|------|--------|
| REST API 服务器 | ✅ 运行中 | 100% |
| Dashboard 可视化 | ✅ 可打开 | 100% |
| 任务 CRUD 操作 | ✅ 正常 | 100% |
| 依赖关系存储 | ✅ 正常 | 100% |
| 基础状态机 | ✅ 正常 | 80% |
| 依赖验证 | ✅ 正常 | 100% |

### ❌ 待实现/有问题功能

| 功能模块 | 状态 | 优先级 | 问题描述 |
|---------|------|--------|----------|
| **环路检测** | ❌ 缺失 | 🔴 P0 | 可能创建循环依赖导致死锁 |
| **拓扑排序** | ❌ 缺失 | 🔴 P0 | 无法确定最优执行顺序 |
| **自动调度器** | ❌ 缺失 | 🔴 P0 | ready 任务不会自动执行 |
| **并发控制** | ❌ 缺失 | 🟡 P1 | 多 Worker 抢占可能冲突 |
| **任务超时** | ❌ 缺失 | 🟡 P1 | running 任务可能永久卡住 |
| **失败重试** | ❌ 缺失 | 🟡 P1 | failed 任务不会自动重试 |
| **优先级队列** | ⚠️ 部分 | 🟡 P1 | 有字段但无调度逻辑 |
| **任务日志** | ❌ 缺失 | 🟢 P2 | 无详细执行日志 |
| **分布式支持** | ❌ 缺失 | 🟢 P2 | 单机版，无法横向扩展 |

---

## 🚨 关键问题清单 (请求 Gemini 老师帮助)

### 问题 1: 循环依赖检测 🔴 P0

**问题描述**:
当前系统允许创建循环依赖，这会导致 DAG 无法执行。

**示例场景**:
```
任务 A → 依赖 任务 B
任务 B → 依赖 任务 C
任务 C → 依赖 任务 A  ❌ 形成循环！
```

**当前行为**:
- API 允许创建这样的任务
- Dashboard 显示会出错（Mermaid 无法渲染环）
- 任务永远无法变为 ready 状态

**需要解决的问题**:
1. 如何在创建任务时检测循环依赖？
2. 应该拒绝创建还是给出警告？
3. 如何检测已存在的循环依赖？

**期望算法**:
```python
def has_cycle(task_id, dependencies, all_tasks):
    """检测添加此任务是否会形成循环"""
    # 需要使用 DFS 或拓扑排序检测
    # 返回 True 如果会形成循环
    pass
```

---

### 问题 2: 任务执行顺序 (拓扑排序) 🔴 P0

**问题描述**:
当有多个 ready 任务时，如何确定最优执行顺序？

**示例场景**:
```
任务 A (ready, priority=2)
任务 B (ready, priority=1)
任务 C (pending, 依赖 A)
任务 D (pending, 依赖 B)
```

**当前行为**:
- 简单按 priority + created_at 排序
- 不考虑 DAG 整体优化

**需要解决的问题**:
1. 如何实现拓扑排序算法？
2. 如何结合优先级和依赖关系？
3. 如何最大化并行度？

**期望算法**:
```python
def topological_sort_with_priority(tasks):
    """
    输入：任务列表（含依赖关系和优先级）
    输出：最优执行顺序
    
    考虑因素：
    1. 依赖关系必须满足
    2. 高优先级任务优先
    3. 最大化并行执行
    """
    pass
```

---

### 问题 3: 自动任务调度器 🔴 P0

**问题描述**:
当前系统没有自动执行 ready 任务的机制，需要手动触发。

**当前架构缺陷**:
```
用户创建任务 → pending
     ↓
依赖完成 → ready
     ↓
❌ 没有自动执行！需要人工调用 API
     ↓
用户手动调用 → running → done
```

**需要解决的问题**:
1. 如何设计后台调度器？
2. 如何避免与 openclaw-gateway 冲突？
3. 使用轮询还是事件驱动？
4. 如何控制并发执行数？

**期望架构**:
```
┌─────────────────────────────────────┐
│      Task Scheduler (后台进程)       │
│                                     │
│  ┌───────────────────────────────┐ │
│  │  监听任务状态变化              │ │
│  │  发现 ready 任务               │ │
│  │  分配到 Worker 执行            │ │
│  └───────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

### 问题 4: 任务超时处理 🟡 P1

**问题描述**:
running 状态的任务可能永远不完成（网络问题、程序崩溃等）。

**示例场景**:
```
任务开始执行 → running
     ↓
网络中断 / 程序崩溃
     ↓
❌ 任务永久卡在 running 状态
     ↓
依赖此任务的其他任务也永久等待
```

**需要解决的问题**:
1. 如何设置合理的超时时间？
2. 超时后自动标记失败还是重试？
3. 如何检测"僵尸"任务？
4. 是否需要心跳机制？

**期望方案**:
```python
class TaskExecutor:
    def execute_with_timeout(self, task, timeout_minutes=30):
        """
        1. 记录开始时间
        2. 定期检查是否超时
        3. 超时后标记为 failed 或 retry
        4. 可选：发送告警通知
        """
        pass
```

---

### 问题 5: 失败重试机制 🟡 P1

**问题描述**:
失败的任务不会自动重试，需要人工干预。

**当前行为**:
```
任务执行 → failed
     ↓
❌ 永久失败，需要人工调用 API 重试
```

**需要解决的问题**:
1. 哪些错误应该重试？（网络错误 vs 逻辑错误）
2. 重试次数限制？
3. 重试间隔策略？（固定间隔 vs 指数退避）
4. 如何区分可重试和不可重试错误？

**期望方案**:
```python
RETRY_CONFIG = {
    'max_retries': 3,
    'backoff_strategy': 'exponential',  # exponential, linear, fixed
    'retryable_errors': ['network', 'timeout', 'rate_limit'],
    'non_retryable_errors': ['validation', 'permission', 'not_found']
}
```

---

### 问题 6: 并发控制与锁机制 🟡 P1

**问题描述**:
多个 Worker 可能同时抢占同一个任务，导致重复执行。

**示例场景**:
```
任务状态：ready
     ↓
Worker A 读取任务
Worker B 读取任务  ❌ 同时读取
     ↓
Worker A 更新为 running
Worker B 也更新为 running  ❌ 重复执行！
```

**需要解决的问题**:
1. 如何原子性地抢占任务？
2. 使用数据库锁还是分布式锁？
3. SQLite 是否支持行级锁？
4. 如何避免死锁？

**期望方案**:
```python
def claim_task_atomic(task_id, worker_id):
    """
    原子性抢占任务
    
    使用 SQL:
    UPDATE tasks 
    SET status = 'running', assigned_worker = ?
    WHERE task_id = ? AND status = 'ready'
    
    检查 rowcount，如果为 0 说明被别人抢走了
    """
    pass
```

---

### 问题 7: Dashboard 实时性 🟢 P2

**问题描述**:
Dashboard 每秒轮询 API，可能不是最优方案。

**当前行为**:
```javascript
setInterval(fetchTasksAndRender, 1000);  // 每秒轮询
```

**问题**:
- 频繁轮询增加服务器负载
- 有 1 秒延迟
- 任务多时渲染慢

**可能的改进方案**:
1. WebSocket 实时推送
2. Server-Sent Events (SSE)
3. 智能轮询（有变化时才更新）

---

### 问题 8: 数据迁移与备份 🟢 P2

**问题描述**:
SQLite 数据库没有自动备份机制，数据可能丢失。

**需要解决的问题**:
1. 如何定期备份 task_queue.db？
2. 如何实现数据库迁移（schema 变更）？
3. 如何清理已完成的历史任务？

---

## 📋 问题优先级总结

| 优先级 | 问题 | 影响 | 预计工作量 |
|--------|------|------|-----------|
| 🔴 P0 | 循环依赖检测 | 系统可能死锁 | 2-4 小时 |
| 🔴 P0 | 拓扑排序 | 执行顺序不优 | 4-6 小时 |
| 🔴 P0 | 自动调度器 | 无法自动执行 | 6-8 小时 |
| 🟡 P1 | 任务超时 | 任务可能卡住 | 2-3 小时 |
| 🟡 P1 | 失败重试 | 需要人工干预 | 3-4 小时 |
| 🟡 P1 | 并发控制 | 可能重复执行 | 2-3 小时 |
| 🟢 P2 | Dashboard 实时性 | 体验问题 | 4-6 小时 |
| 🟢 P2 | 数据备份 | 数据安全问题 | 2-3 小时 |

---

## 🎯 请求 Gemini 老师帮助的具体问题

### 核心算法问题

1. **循环依赖检测算法**
   - 如何在创建任务时高效检测是否会形成循环？
   - 使用 DFS 还是并查集？
   - 时间复杂度如何？

2. **带优先级的拓扑排序**
   - 标准拓扑排序很简单，但如何结合优先级？
   - 如何在满足依赖的前提下最大化并行度？
   - 有没有现成的算法或库推荐？

3. **任务调度算法**
   - 如何设计一个高效的后台调度器？
   - 轮询间隔多少合适？
   - 如何使用事件驱动代替轮询？

### 架构设计问题

4. **并发控制**
   - SQLite 的行级锁如何使用？
   - 是否需要引入 Redis 做分布式锁？
   - 如何设计幂等性保证？

5. **超时与重试**
   - 超时时间应该动态还是静态？
   - 指数退避的最佳实践是什么？
   - 如何区分错误类型？

### 工程实践问题

6. **测试策略**
   - 如何测试 DAG 的正确性？
   - 需要哪些单元测试和集成测试？
   - 如何模拟并发场景？

7. **监控与告警**
   - 需要监控哪些指标？
   - 如何设置告警阈值？
   - 日志应该记录什么？

---

## 💬 给 Gemini 老师的留言

> 你好！我正在构建一个基于 SQLite 的 DAG 任务调度系统，目前基础功能已完成，但遇到几个算法和架构设计问题。
>
> **系统特点**:
> - 单机版，SQLite 数据库
> - 支持任务依赖关系（DAG）
> - Python HTTP Server + Mermaid.js 可视化
> - 目标：自动化工作流调度
>
> **最紧急的问题**:
> 1. 循环依赖检测算法
> 2. 带优先级的拓扑排序
> 3. 自动任务调度器设计
>
> 希望能得到算法建议、代码示例或最佳实践指导！谢谢！🙏

---

**报告生成者**: [YOUR_AI_NAME] 🧠  
**联系方式**: VectorBrain System  
**期望回复时间**: 尽快（用户在线等待）

---

*此文档已保存至：`~/.vectorbrain/dag/DAG 系统问题诊断报告.md`*
